This project  is an extended version of Tic Tac toe game where we need 5 coins in a row 
to win. The board is also of an infinite size and it keeps growing.

The code is written in C# using Visual Studio and the core logic of the algorithm is in the
file ComputerMoveHandler.cs

The source code can be downloaded and executed using Visual Studio 2010